{
    "id": "uv",
    "version": "1.0.0",
    "name": "uv",
    "description": "Installs uv — an extremely fast Python package and project manager. Centralises Python interpreter and package caches under a named Docker volume for faster rebuilds.",
    "documentationURL": "https://github.com/stacit-ai/devcontainer-features/tree/main/src/uv",
    "options": {
        "version": {
            "type": "string",
            "default": "latest",
            "description": "Version of uv to install (e.g. '0.6.0'). Use 'latest' for the current release."
        }
    },
    "mounts": [
        {
            "source": "uv-${devcontainerId}",
            "target": "/opt/uv",
            "type": "volume"
        }
    ],
    "containerEnv": {
        "UV_PYTHON_INSTALL_DIR": "/opt/uv/python",
        "UV_CACHE_DIR": "/opt/uv/cache",
        "UV_PROJECT_ENVIRONMENT": "/opt/uv/venv"
    },
    "installsAfter": []
}
